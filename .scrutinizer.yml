build:
  tests:
    override:
      - 'ls -al'
      -
        command: 'bin/phpspec run -f progress -c phpcpec-ci.yml'
        coverage:
          file: 'coverage.clover'
          format: 'php-clover'